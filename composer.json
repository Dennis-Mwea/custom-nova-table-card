{
    "name": "dennis/custom-table",
    "description": "Nova custom table card. Show your latest data (orders, posts, users...) as card or data you prefer.",
    "keywords": [
        "laravel",
        "nova"
    ],
    "license": "MIT",
    "require": {
        "php": ">=7.1.0"
    },
    "autoload": {
        "psr-4": {
            "Dennis\\CustomTable\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Dennis\\CustomTable\\CardServiceProvider"
            ]
        }
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
